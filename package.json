{
  "name": "aero-slider",
  "version": "1.0.0-beta.6",
  "description": "A lightweight, CSS-first slider library for the modern web",
  "type": "module",
  "workspaces": [
    "site"
  ],
  "main": "./dist/aero-slider.min.js",
  "module": "./dist/aero-slider.min.js",
  "types": "./dist/aero-slider.d.ts",
  "exports": {
    ".": {
      "types": "./dist/aero-slider.d.ts",
      "import": "./dist/aero-slider.min.js",
      "default": "./dist/aero-slider.min.js"
    },
    "./slider.css": "./dist/aero-slider.min.css"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "cd site && bun run dev",
    "site:dev": "cd site && bun run dev",
    "site:build": "cd site && bun run build",
    "prepare": "bun run build",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "pack": "bun pm pack"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "esbuild": "^0.27.3",
    "prettier": "^3.4.2",
    "tsup": "^8.5.1",
    "typescript": "^5"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
